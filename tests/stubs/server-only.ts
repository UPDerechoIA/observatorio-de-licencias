// Stub vacío para los tests: en el entorno de Vitest no existe la condición
// "react-server", por lo que el paquete real `server-only` lanzaría. Lo
// reemplazamos por este módulo inocuo solo durante los tests.
export {};
